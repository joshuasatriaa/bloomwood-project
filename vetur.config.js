module.exports = {
  // **optional** default: `{}`
  // override vscode settings part
  // Notice: It only affects the settings used by Vetur.
  // **optional** default: `[{ root: './' }]`
  // support monorepos
  projects: [
    "./admin", // shorthand for only root.
    // "./client",
  ],
};
